/pour effectuer la compilation on a utilisé les commandes suivantes:

make : permet de compiler l'exécutable dans le dossier bin/ .
make clean : permet de supprimer tout ce qui a été créé pendant la compilation .
make tests : permet de compiler l'exécutable des tests unitaires dans le dossier tests/ .
make doc : sert a génerer la documentation dans le doxyfile .
